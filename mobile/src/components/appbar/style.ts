import { StyleSheet } from "react-native";

export const style = StyleSheet.create({
  container: {
    backgroundColor: '#fd6d41',
    paddingHorizontal: 0,
    height: 80
  },
  content: {
    alignItems: "center",
    justifyContent: 'space-between',
  }
})